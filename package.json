{
    "name": "root",
    "private": true,
    "scripts": {
        "clean": "npm run clean --prefix=./app && npm run clean --prefix=./lib",
        "link": "lerna bootstrap",
        "build": "npm run build:lib && npm run build:app",
        "build:app": "npm run build --prefix=./app",
        "start:app:prod": "npm run start:prod --prefix=./app",
        "build:lib": "npm run build --prefix=./lib",
        "publish:lib": "npm publish lib"
    },
    "devDependencies": {
        "lerna": "~3.18.3",
        "rimraf": "~3.0.0"
    }
}
